---
pagination:
  data: collections.posts
  size: 3
  alias: list
  reverse: true
permalink: '<% if (pagination.pageNumber > 0) { %><%= `page${pagination.pageNumber + 1}/index.html` %><% } else { %>index.html<% } %>'
eleventyComputed:
  title: '<% if (pagination.pageNumber > 0) { %><%= `Page ${pagination.pageNumber + 1} | ${site.title}` %><% } else { %><%= site.title %><% } %>'
---

Welcome to another personal website

<h2>What i do?</h2>

<p>
  🧑‍💻 White code on <a href="https://github/izzqz">GitHub</a><br>
  📝 Trying to dev <a href="/blog">Blog</a><br>
  📺 Streaming my life on <a href="https://twitch.tv/izzqz">Twitch</a> <b>[🇷🇺/🇬🇧]</b><br>
</p>

<h2>My last posts</h2>

<ul>
  <% list.forEach((post) => { %>
  <li>
    <a href="<%= post.url %>" class="text-link"><%= post.data.title %></a>
    
    <div><%= this.dateFns.format(new Date(post.data.date), 'LLLL d, yyyy') %></div>
  </li>
  <% }) %>
</ul>

<div>
  <% if (pagination.previous) { %>
  <div>
    <a href="<%= pagination.previous.replace(/index.html$/, "") %>">← Newer Posts</a>
  </div>
  <% } %> <% if (pagination.next) { %>
  <div>
    <a href="<%= pagination.next.replace(/index.html$/, "") %>">Older Posts →</a>
  </div>
  <% } %>
</div>

<h2>Pet Projects</h2>

<ul>
  <li>
    <a href="https://github.com/izzqz/dotfiles" class="text-link">Project name</a>
    <div>Description</div>
  </li>
</ul>

<h2>Other</h2>

<p>
  I also have <a arrow href="https://keybase.io/izzqz">keybase</a> with extra identities and my <a arrow href="https://keybase.io/izzqz/pgp_keys.asc">PGP key</a>.
  If you want to help me see <a href="/donate">Donation page</a>
</p>
